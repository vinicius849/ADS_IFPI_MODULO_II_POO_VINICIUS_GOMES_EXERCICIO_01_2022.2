import math

class Circulo {
    raio : number = 0;  // A classe Círculo sendo a delimitação de uma circunferência, eu defino o meu atributo raio.
};                      // Meu círculo é dado como o valor de um terreno ou imóvel a ser comprado pelo dono.

constructor { (
        raio.areaCirculo()
        console.log("Crio um método para calcular a áera da minha classe círculo.");
        raio.perimetroCirculo()
        console.log("Crio um segundo método para calcular o perímetro da minha classe círculo, que será o comprimento.");
    ) }

let circulo : {
    nome : string = "terreno do Vinícius.";   // O formato do meu círculo será instanciado para determinado contexto.
    ano : number = 2022;
    preco : number = 80000;
    raio : number = 5;
    pi : math.round() : Number += 3.15;
};

calcularAreaCirculo() {
    return pi * (raio ** 2);  // Isto é 3.15 * (5 ** 2) => ao quadrado é a fórmula da área do círculo: pi * (r ** 2)
}
console.log("Chamo uma função para calcular a áera do meu círculo por meio da atribuição dos valores acima.");

calcularPerimetroCirculo() {  
    return (2 * pi * raio);  //  O perímetro é o comprimento da circunferência, que vale (2 * pi * r).
}
console.log("Deste modo e por último, depois de calcular a área do círculo, eu determino o comprimento da circunferência ou perímetro.");
console.log(circulo.calcularPerimetroCirculo());













    



